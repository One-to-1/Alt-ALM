# Raw subagent research reports

Unedited final reports from the research subagents, persisted verbatim as provenance for the
synthesized documents in `docs/research/`. The synthesized docs supersede these; when they disagree,
the synthesized doc (which reconciles siblings and live-probe evidence) wins.

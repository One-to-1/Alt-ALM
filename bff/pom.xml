<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<!-- Boot 4.1.0 is the current GA (latest in Maven Central) and supports JDK 25.
		     NOTE: Spring Initializr's /metadata/client reports legacy ids like "4.1.0.RELEASE";
		     the published artifact version is plain "4.1.0" (the .RELEASE suffix was dropped
		     after Boot 2.x). ADR 0002 requires 4.0.x or later - Boot 3.x predates JDK 25. -->
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>ai.surgeone.altalm</groupId>
	<artifactId>bff</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>bff</name>
	<description>Alt-ALM BFF - the single enforcement point for ALM write hazards</description>

	<properties>
		<!-- JDK 25 LTS per ADR 0002. Do NOT enable preview features. -->
		<java.version>25</java.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Contract tests hit the live ALM sandbox and need Secrets/, so they are excluded from
		     the default build and from CI (test-strategy §7: every-commit layers must never require
		     credentials). Opt in with `./mvnw test -Pcontract`. -->
		<excluded.test.groups>contract</excluded.test.groups>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<!-- Boot 4 modularized JSON: starter-webmvc no longer pulls Jackson in transitively.
		     We need it explicitly and directly, because the write-safety layer builds ALM's
		     Fields array as an ordered Jackson tree rather than relying on POJO mapping
		     (field order is load-bearing - see AlmEntityBody). -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-json</artifactId>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<excludedGroups>${excluded.test.groups}</excludedGroups>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.springframework.boot</groupId>
									<artifactId>spring-boot-configuration-processor</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- `./mvnw test -Pcontract` runs the live-sandbox suite as well. It still gates on the
		     credentials file being present and SKIPS (never fake-passes) when it is absent. -->
		<profile>
			<id>contract</id>
			<properties>
				<excluded.test.groups/>
			</properties>
		</profile>
	</profiles>

</project>
